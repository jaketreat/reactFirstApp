import logo from "../logo.svg";

function Logo({ imageCLicked }) {

    return (
        <div>
            <img 
                src={logo} 
                alt="Logo"
    
            />
        </div>
    )
}

export default Logo;